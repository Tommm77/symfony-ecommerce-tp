{% extends 'base.html.twig' %}

{% block title %}CartsProducts{% endblock %}

{% block body %}
    <h1>CartsProducts</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ carts_product.id }}</td>
            </tr>
            <tr>
                <th>Quantity</th>
                <td>{{ carts_product.quantity }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_carts_products_index') }}">back to list</a>

    <a href="{{ path('app_carts_products_edit', {'id': carts_product.id}) }}">edit</a>

    {{ include('carts_products/_delete_form.html.twig') }}
{% endblock %}
